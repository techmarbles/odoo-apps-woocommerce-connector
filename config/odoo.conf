[options]
addons_path = /mnt/extra-addons
data_dir = /var/lib/odoo

; db connection — kept here (not only in compose env) so that
; `docker compose exec odoo odoo ...` works the same as the container's own startup
db_host = db
db_port = 5432
db_user = odoo
db_password = odoo

admin_passwd = admin
